<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sifang.insurance.calculator.mapper.PremiumRuleMapper">
    
    <select id="selectByProductIdAndStatus" resultType="com.sifang.insurance.calculator.entity.PremiumRule">
        SELECT * FROM ins_premium_rule 
        WHERE product_id = #{productId} 
        AND status = #{status} 
        ORDER BY sort ASC
    </select>
    
    <select id="selectByProductIdAndType" resultType="com.sifang.insurance.calculator.entity.PremiumRule">
        SELECT * FROM ins_premium_rule 
        WHERE product_id = #{productId} 
        AND rule_type = #{ruleType} 
        AND status = 1 
        ORDER BY sort ASC
    </select>
</mapper>